import elements from "./elements";
import features from "./features";
import pages from "./pages";
import providers from "./providers";
import restrictedFeatures from "./restrictedFeatures";

const components = {
    elements,
    features,
    pages,
    providers,
    restrictedFeatures
};

export default components;
