/**
 * RealtimeStatusElement props
 */
export interface RealtimeStatusElementProps {
    /**
     * Label shown next to the icon, defaults to the translated offline label
     */
    label?: string;

    /**
     * Tooltip text, defaults to the translated offline explanation
     */
    title?: string;

    /**
     * Additional class names
     */
    className?: string;
}
