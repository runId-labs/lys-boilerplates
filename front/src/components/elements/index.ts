import {activeFilterBadgesElementConfig} from "./ActiveFilterBadgesElement/translations";
import {paginationElementConfig} from "./PaginationElement/translations";
import {textareaWithAiElementConfig} from "./TextareaWithAiElement/translations";
import {sortDropdownElementConfig} from "./SortDropdownElement/translations";
import {inputElementConfig} from "./InputElement/translations";
import {colorPickerElementConfig} from "./ColorPickerElement/translations";
import {quickSearchInputElementConfig} from "./QuickSearchInputElement/translations";
import {tableElementConfig} from "./TableElement/translations";
import {fileDropZoneElementConfig} from "./FileDropZoneElement/translations";
import {realtimeStatusElementConfig} from "./RealtimeStatusElement/translations";

const elements = {
    activeFilterBadgesElement: activeFilterBadgesElementConfig,
    paginationElement: paginationElementConfig,
    textareaWithAiElement: textareaWithAiElementConfig,
    sortDropdownElement: sortDropdownElementConfig,
    inputElement: inputElementConfig,
    colorPickerElement: colorPickerElementConfig,
    quickSearchInputElement: quickSearchInputElementConfig,
    tableElement: tableElementConfig,
    fileDropZoneElement: fileDropZoneElementConfig,
    realtimeStatusElement: realtimeStatusElementConfig
};

export default elements;
