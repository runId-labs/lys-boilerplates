# API Endpoint (using proxy - see vite.config.js)
VITE_GRAPHQL_ENDPOINT=/graphql

# SSE Endpoint (using proxy - see vite.config.js)
# In production, set to the full URL of the API server
# VITE_SSE_ENDPOINT=https://api.example.com
